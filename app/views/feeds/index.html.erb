<%= form_for Feed.new do |f| %>
  <%= f.text_field :url %>
  <%= f.submit 'create' %>
<% end %>

<table>
<% @feeds.each do |feed| %>
  <tr>
    <td><a href="<%= feed.url %>"><%= feed.title %></a></td>
  </tr>
<% end %>
</table>